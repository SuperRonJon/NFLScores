version: '3'
services:
  web: 
    build: .
    ports: 
      - "8000:8000"
  db:
    image: "mongo"
    volumes:
      - nflscores-data:/data/db
  proxy:
    build: ./proxy
    ports:
      - "80:80"


volumes:
  nflscores-data: